<Window x:Class="TaskСontrol.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskСontrol"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            <Menu Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
                <MenuItem Header="Меню" Height="30" FontSize="14">
                    <MenuItem Header="Добавить задание" Name="AddTask" Click="AddTask_Click"/>
                    <MenuItem Header="Выход"/>
                </MenuItem>
            </Menu>
            <TreeView Grid.Row="1" Grid.Column="0" Name="TreeViewTask"/>
            <ListBox Grid.Column="1" Grid.Row="1" Name="ListBoxDetail"/>

        </Grid>
    </Grid>
</Window>
